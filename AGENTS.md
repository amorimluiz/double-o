# AGENTS.md — double-o

Instruções específicas deste projeto. A governança global vive em [`instructions/AGENTS.md`](instructions/AGENTS.md) e vale integralmente aqui; este arquivo apenas a complementa.

## Repositório remoto

- O repositório é hospedado no **GitHub**: `amorimluiz/double-o` (remote `origin`, branch padrão `main`).
- Toda interação com o repositório remoto no GitHub — issues, pull requests, releases, Actions, API — é feita via CLI **`gh`**.
- Operações de versionamento local continuam via `git` (commit, branch, push, pull); o `gh` cobre o que for do GitHub.
